# PBIP Sentinel (pbiscan) — v1.2 Roadmap & Calibration Plan

**Status**: Planning & Methodology Specification  
**Preceding Milestone**: `v1.1.0` Shipped & Frozen (Commit `c02faa6`, 151/151 Tests Passing)  
**Governing Principle**: **Infrastructure ≠ Rule ≠ Product Claim**. Do not implement new detection rules or expand the rule matrix until empirical real-world validation data proves existing accuracy and identifies true gaps.

---

## 1. Current State Baseline (v1.1.0)

| Layer | State | Details |
|---|---|---|
| **Extraction** | Frozen | TMDL / TMSL semantic models & PBIR / JSON report layout extraction |
| **Canonical Model** | Extended | `ModelGraph` (Topology infrastructure), `DaxDictionary`, `DaxDependencyGraph` (Multi-hop transitive reachability), `ReportDOM` |
| **Active Rules** | Locked (11 Rules) | 5 Model (`M001`–`M005`), 4 DAX (`D001`–`D004`), 2 Report (`R001`–`R002`) |
| **Suppression** | Active | Declarative via `pbiscan.suppressions.json` (score-neutral, auditable) |
| **Test Suite** | Active | **151 passed, 0 failed** (118 baseline regression + 33 v1.1 unit/golden tests) |
| **Studio & Web** | Active | Live workbench at [pbip-sentinel.netlify.app](https://pbip-sentinel.netlify.app/) |

---

## 2. Three-Phase Execution Strategy for v1.2

```text
v1.1.0 (FROZEN)
      │
      ▼
PHASE 0: REAL-WORLD PBIP AUDIT & BENCHMARKING
      ├── Multi-project batch scan (small, medium, enterprise PBIPs)
      ├── Finding classification matrix (True Positive, False Positive, False Negative, Ambiguous)
      └── Scan latency & memory profiling
      │
      ▼
PHASE 1: RULE CALIBRATION & RULE PROPOSAL GATES
      ├── Heuristic threshold calibration (D001 patterns, R001/R002 thresholds)
      ├── Formal evaluation of topology candidate rules (M006 Isolated Table, M007 Ambiguous Path)
      └── Golden fixture specification before code
      │
      ▼
PHASE 2: v1.2 IMPLEMENTATION & VALIDATION
      ├── Calibrated rule updates / approved new rules
      ├── 151+ regression guard verification
      └── Studio UI & Documentation release
```

---

## 3. Phase 0: Real-World PBIP Audit

Before writing any new rule code, `pbiscan` must be benchmarked across a diverse corpus of real-world Power BI projects.

### 3.1 Project Corpus Categories
1. **Small Single-Table Models**: Flat tables, quick prototypes, basic visuals (<5 MB).
2. **Standard Star Schemas**: 1 Fact + 4-6 Dimensions, clean one-to-many active relationships (10–50 MB).
3. **Complex Enterprise Models**: Multiple facts, bridge tables, role-playing dimensions, bidirectional cross-filtering, extensive measure chains (50–500+ MB).
4. **Calculations-Heavy Models**: Financial reporting models with 100+ DAX measures, time-intelligence hierarchies, and semi-additive KPIs.

### 3.2 Finding Classification Taxonomy
For every finding produced during the audit, categorize as follows:

| Classification | Definition | Action Required |
|---|---|---|
| **True Positive (TP)** | Legitimate anti-pattern, performance risk, or redundancy correctly detected. | Document frequency across models. |
| **False Positive (FP)** | Flagged object is intentional, valid, or falsely identified due to pattern limitations. | Refine regex/AST boundaries or calibrate confidence score down. |
| **False Negative (FN)** | Known model defect or high-risk pattern present in PBIP that was missed by scanner. | Identify missing signal; formulate candidate rule. |
| **Ambiguous (AMB)** | Detection is structurally correct, but operational impact depends on data volume/engine mode. | Ensure remediation prose and confidence percentage accurately hedge severity. |

---

## 4. Phase 1: Rule Calibration & Candidate Gates

### 4.1 Topology Rules Evaluation (`M006` / `M007`)
Topology infrastructure (`isolated_tables()`, `relationship_paths()`, `connected_components()`) was shipped in `canonical/` in v1.1.0. Candidate rules will only be promoted to the active rule table if they meet strict quality gates:

- **Candidate `M006` (Isolated / Disconnected Table)**:
  - *Criteria*: Must distinguish disconnected dimension/fact islands from legitimate disconnected calculation groups, what-if parameter tables, or DAX disconnected slicers (e.g. harvesting tables).
  - *Negative Fixture Required*: Models with What-If parameter tables and calculation groups must not trigger false alarms.
- **Candidate `M007` (Ambiguous Filter Path)**:
  - *Criteria*: Must distinguish active multi-path loops from valid star-schema multi-grain hierarchies.
  - *Negative Fixture Required*: Single-path star schemas and inactive relationship networks must produce zero findings.

### 4.2 DAX Reference Engine Calibration
- Evaluate edge cases where DAX measures reference columns matching measure names (`Table[Name]` vs `[Name]`).
- Evaluate comments and string literals containing measure names to minimize false reference edges.

---

## 5. Phase 2: Implementation & Quality Gates

1. **Golden Tests First**: Any new or modified rule must have its positive and negative golden fixtures committed before implementation.
2. **Zero-Regression Mandate**: All **151 existing tests** must continue to pass unchanged.
3. **Additive Contracts**: Schema modifications to `AuditIssue` or `CanonicalReport` must maintain full backwards compatibility.
4. **No LLM in Core Detection**: Core rule evaluations remain 100% static, deterministic, and auditable.

---

*Authored for PBIP Sentinel Engineering & Architecture Governance.*
