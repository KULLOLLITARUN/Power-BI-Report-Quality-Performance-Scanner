table DimDate
	dataCategory: Time

	column Date
		dataType: dateTime
		sourceColumn: Date
		isKey

	column Year
		dataType: int64
		sourceColumn: Year

	partition DimDate = m
		mode: import
		source =
			let
			    Source = #table({"Date", "Year"}, {})
			in
			    Source
